# Keep empty for local development so frontend calls stay relative, for example /api/test
VITE_API_BASE_URL=""

# Vite dev proxy forwards /api requests to the local backend
VITE_API_PROXY_TARGET="http://127.0.0.1:5222"
